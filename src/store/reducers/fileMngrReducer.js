// import * as actionTypes from '../actions';

const intitialState = {};

const fileMngrReducer = (state = intitialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default fileMngrReducer;
